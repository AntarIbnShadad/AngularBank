import { TransferDepositPipe } from './transfer-deposit.pipe';

describe('TransferDepositPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferDepositPipe();
    expect(pipe).toBeTruthy();
  });
});
